#:kivy 1.0.9
<MyToolbar>:
    BoxLayout:
        ToolbarButton:
            image: 'createhouse'
            callback: root.create_house
#        ToolbarButton:
#            image: 'image'
#            callback: root.create_image
#        ToolbarButton:
#            image: 'video'
#            callback: root.create_video

[ToolbarButton@Image]:
    # This is the same as before
    source: 'resources/toolbar/%s.png' % ctx.image
    size: 50, 50
    size_hint: None, None

    # Now, we are using the ctx for the variable part of the template
    on_touch_down: self.collide_point(*args[1].pos) and self.callback()

<House>:
	image: house_image
	size: 100, 100
	Image:
		id: house_image
		source: 'resources/house.jpg'

<GentriCityGame>:
#	house: house_sprite
	size: 800, 600
	Image:
		size: 800, 600
		source: 'resources/background_far.jpg'

	MyToolbar:
	    size: 800, 10
	    top: root.top - 100
        rgba: 1,0,0,1

#    Label:
#        font_size: 70
#        center_x: root.width * 1 / 2
#        top: root.top - 50
#        text: "Gentrification City Game"
#
#    House:
#    	id: house_sprite
#    	center: root.width * 1 / 2, root.center_y * root.height * 0.1